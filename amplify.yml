version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build-production
  artifacts:
    baseDirectory: dist/noise-to-help-app
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
